# 2026-SP W13 检查报告：W12 落实验收 + 新发现问题

> 检查日期：2026-09-14（晚，第二轮）
> 验收对象：commit c2a3416 "W12: check-report response - 9/9 action items dispositioned"
> 总体验收：**9/9 中 7 项验收通过，A2 只解决了一半（发现新矛盾），另有 5 个新问题**。W12 的各向异性分辨率更正（0.0417° ≈ 3.6×4.6 km）经复算确认正确。

---

## 一、验收通过项（保持，不重复展开）

| 项 | 验收结论 |
|---|---|
| A1 分辨率 | ✅ 各向异性更正数学上正确（0.0417°×111.32×cos40.3°≈3.5–3.6 km 东西；4.63 km 南北），README/大纲/摘要已统一 |
| A3 双口径 | ✅ README 三句话已双标注（pooled 0.020–0.061 主，折级 0.036–0.088 辅） |
| A4 图表编号 | ✅ fig01–08 诊断线 + fig09–13 混合线，diagnostic_reserve 归档旧图 |
| A5/B1 注册表与数据集定位 | ✅ 大纲 2.5 写清三层角色，README 降级 |
| B2 第二目标复检 | ✅ 实验做了、1552 网格与主线零重叠确认、诚实标注目标为模型反演产物 |
| B3 管理假说路径 | ✅ 排序落地，路径 (c) 放弃且理由已写入正文 |
| C1/B4 | ✅ 摘要多种子同步、目标 B 上界升格、D 目标 6.5.1 正文化 |

## 二、验收未通过/新发现问题

### N1.【重要】A2 只解决了一半：因子实验与权威台账的折级口径仍矛盾
- 因子实验 `w12_a2_latblock_factorial.csv`：lat_q4 × optuna428 的 **fold_mean = −0.2133 ± 0.3203**（pooled 0.1194）
- 权威台账 `review_response_diag_seeds.csv`：lat_block XGB **R2_mean = 0.1127 ± 0.0061**（by_seed 0.105/0.114/0.119）
- 同一设计（q4 + optuna + C_blend）折级口径相差 0.33。唯一自洽的解释是：权威台账的 R2_mean 是**逐种子 pooled 的均值**，而它**没有 pooled 列、也没有折级统计列**——列含义未声明。
- 后果：若折级口径在权威配置下实为 −0.21，则"纬度块崩塌"在权威配置下比旧口径更严重，大纲 5.x 的叙事和 3.2.4 的解读都要按权威口径重写；"A2 已解决"的声明目前只覆盖了 pooled 端。
- **行动**：(1) 权威台账 CSV 增加显式列（pooled_mean±std、fold_mean±std）并声明每列含义；(2) 用权威配置重跑并核对 fold 端；(3) 大纲 5.2/5.3 与 6.5.1 的 fold-vs-pooled 解释扩展到 lat4（234 样本 4 块，折级方差巨大，与 D 目标同机制）。

### N2. 大纲 3.3 仍是旧参数，与权威台账冲突
- 大纲 3.3："XGBoost(200, depth=5, lr=0.05)"；权威台账声明 optuna428 参数。
- 大纲 3.2.4 仍以折级口径 (+0.036~0.088) 作为行文主数字，而 README 已改为 pooled 主口径——同一文档内主次颠倒。
- **行动**：3.3 更新为权威参数（旧参数可作灵敏度附注）；3.2.4/摘要/Discussion 统一 pooled 主口径。

### N3. README「关键数据」表仍推荐已废弃台账
- 表中仍列 `cv_scheme_comparison.csv` 与 `cv_optimism_gap.csv`，违反 `CV_LEDGER_CANONICAL.md` "旧 csv 不得再被新增引用"的自定规则；且 `cv_optimism_gap.csv` 本身只有折级口径一列，pooled 主口径在台账层无处落地。
- **行动**：README 表替换为权威台账 + 因子实验；cv_optimism_gap.csv 重生成（pooled+fold 双列）或标 DEPRECATED。

### N4. 工作区未收尾（"修改已完成"但提交未完成）
- 14 个图表重命名（D + ??）与 `.gitignore` 修改仍停留在工作区，未提交；
- `Data/2021ChinaWheatYield30m.tif` **1.1 GB** 未跟踪——**绝不可直接 git add**：加入 .gitignore，改为在数据注册表记录来源 URL/DOI + SHA256 + 下载命令；
- 仓库根目录 13 个 `_*.py` 临时脚本未跟踪——移入 `src/utils/` 或 `scratch/`（加 README），否则复现者无法分辨哪些是管线。
- **行动**：一次性提交图表重命名；gitignore 大文件；清理根目录。

### N5. fig08 与 fig09–13 缺 tiff，与 README 声明不符
- README 写 "fig01-13，600dpi png+tiff"，实际 fig08_variogram 与 fig09–13 仅有 png。
- **行动**：补生成 tiff（投稿需 tiff），或修正 README 表述为"诊断线 01–07 tiff"。

### N6.【提问】6.7 面板目标的作物身份与单位需要确认
- New_Yield = **15.71±0.31 t/ha**——本区冬小麦现实产量为 5–7 t/ha，15.7 更像夏玉米或小麦+玉米周年总产，或单位不同。
- 若非冬小麦，6.7 的"第二目标复检"实为**跨作物**检验：作为"目标来源决定表观性能"的旁证依然成立（甚至更强），但不能被解读为跨季节/同年份复检，6.7 与 8.5 的措辞需相应收窄。
- **行动**：核对 Xiao2024 数据字典确认 New_Yield 的作物与单位；在 6.7 首句明示。

### N7.【提问，沿袭】期刊终选仍悬置
- README badge 仍为 "Agronomy / Field_Crops_Res"。两线图号已统一、证据链已齐，写作口吻受目标期刊影响（MDPI 结构 vs Elsevier 叙事），建议 W13 内由导师拍板，否则 Methods/Discussion 会被迫返工。

## 三、W13 行动清单

| # | 行动 | 对应 | 预计 |
|---|---|---|---|
| 1 | 权威台账补 pooled/fold 双列并重跑核对 lat4 折级端 | N1 | 0.5d |
| 2 | 大纲 3.3 参数更新 + 全文 pooled 主口径统一 | N2 | 1h |
| 3 | README 关键数据表替换权威台账；gap CSV 重生成 | N3 | 0.5h |
| 4 | 提交图表重命名；gitignore 1.1GB tif；清理根目录脚本 | N4 | 1h |
| 5 | 补 fig08/09–13 tiff 或修正 README | N5 | 0.5h |
| 6 | 确认 New_Yield 作物/单位并改写 6.7 首句 | N6 | 0.5h |
| 7 | 导师确认期刊终选 | N7 | 咨询 |
